# Keep the build context small; the Dockerfile copies only what it names,
# but everything not ignored here is still uploaded as context.
node_modules
**/node_modules
client
.git
.env*
!.env.example
*.md
storybook-static
.next
