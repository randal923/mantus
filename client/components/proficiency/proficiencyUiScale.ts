/**
 * Zoom applied to the OTClient proficiency art (native sizes come from
 * proficiency.otui: 108px columns, 80px tiles). 1.25 keeps seven columns
 * plus the item panel inside the extra-wide modal.
 */
export const PROFICIENCY_UI_SCALE = 1.25;
