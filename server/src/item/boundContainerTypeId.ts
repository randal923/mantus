/**
 * The bound container ("bound items"): Canary's store-inbox container, one
 * per character in the `bound` equipment slot. Its direct children are
 * character-bound — the loot pouch every character owns, and the Portable
 * Seller once purchased.
 */
export const BOUND_CONTAINER_TYPE_ID = 23_396;
