{
  "name": "tibia",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "protocol"
  ],
  "scripts": {
    "dev": "concurrently -k -n next.js,server -c cyan,magenta \"yarn workspace client dev\" \"yarn workspace server dev\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "typecheck": "yarn workspace @tibia/protocol typecheck && yarn workspace server typecheck && yarn workspace client typecheck",
    "test": "yarn workspace server test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
