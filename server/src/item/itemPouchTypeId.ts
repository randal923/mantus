/**
 * The Item Pouch (this server's take on Canary's Gold Pouch): a store-bought
 * container with slots that never run out. While one sits anywhere inside the
 * equipped backpack tree, destination-less loot moves (auto loot, quick loot,
 * hand loot without an aimed slot) land in it first — see
 * `plan/planItemPouchPlacement.ts`.
 */
export const ITEM_POUCH_TYPE_ID = 23_721;
