/**
 * Canary gold_converter.lua registers the store's Gold Converter (23722) and
 * its untradeable daily-reward twin, the temporary gold converter (25719).
 */
export const GOLD_CONVERTER_TYPE_IDS: ReadonlySet<number> = new Set([
  23722, 25719,
]);
