/**
 * The Adventurer's Stone (Canary 16277): every character owns exactly one,
 * inside the bound root. Used in a city temple it teleports to the
 * Adventurers Guild and back — see `action/AdventurersStoneService.ts`.
 */
export const ADVENTURERS_STONE_TYPE_ID = 16_277;
