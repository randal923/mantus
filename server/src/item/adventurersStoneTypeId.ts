/**
 * The Adventurer's Stone (Canary 16277): every character owns exactly one,
 * inside the bound root. Its temple-teleport use effect is still deferred —
 * see the `adventurersStone` entry in canary-world-action-parity.json.
 */
export const ADVENTURERS_STONE_TYPE_ID = 16_277;
