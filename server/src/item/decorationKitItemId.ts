/**
 * Canary's ITEM_DECORATION_KIT: the wrapped parcel a store-bought piece of
 * house furniture arrives as. The kit row carries an `unwrapTo` attribute
 * naming the furniture type it turns into on an owned house tile.
 */
export const DECORATION_KIT_ITEM_ID = 23_398;
